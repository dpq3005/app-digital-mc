import { PinDigitDirective } from './pin-digit.directive';

describe('PinDigitDirective', () => {
  it('should create an instance', () => {
    const directive = new PinDigitDirective();
    expect(directive).toBeTruthy();
  });
});
